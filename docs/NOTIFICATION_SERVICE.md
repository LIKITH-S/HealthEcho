# Notification Service

- Handles SMS (Twilio), Email, Push (Firebase) and emergency alerts.
- Provide handlers in `notification_service/handlers` and backend `utils/notificationService.js` should delegate to them.

(Placeholder — include config for providers and rate limits.)
