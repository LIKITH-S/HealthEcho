# Drug Checker Guide

- Uses drug interaction database to detect harmful interactions.
- Endpoint: POST /api/drugInteraction/check with list of drugs.
- Use a curated DB (seed `database/seeds/drug_interaction_database.sql`).

(Placeholder — add data sources and legal disclaimers.)
